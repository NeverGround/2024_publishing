// - 0 +

const minus_btn = document.createElement("botton");
const plus_btn = document.createElement("button");
const Number_btn = document.createElement("button");

minus.innerText = "-";
minus.addEventListener("click", () => {
  minus.innerText = minus;
});
plus.innerText = "+";
plus.addEventListener("click", () => {
  plus.innerText("click", () => {
    plus_btn.innerText = plus;
  });
});
Number.innerText = "0";
Number.addEventListener("click", () => {
  Number.addEventListener,
    ("click",
    () => {
      Number_btn.innerText = Number;
    });
});
